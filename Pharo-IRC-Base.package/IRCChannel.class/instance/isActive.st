testing
isActive
	"If a channel is active, it will
	receive relevant server/connection level
	messages so the user can see them. This exists
	so someone in a given channel view can still see
	things like notices, errors, etc that are not
	channel specific."
	^ active